let initialState = {
    friends: [
        {id: 1, name: 'Evelina'},
        {id: 2, name: 'Izyum'},
        {id: 3, name: 'Barsik'}
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;