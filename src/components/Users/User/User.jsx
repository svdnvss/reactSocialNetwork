import s from './User.module.css'

const User = (props) => {
    /*
        let follow = () => {
            props.followIt();
        }

        let unfollow = () => {
            props.unfollowIt();
        }

        let onFollowUnfollowClick = () => {
            if (props.isFollowed === true)
                unfollow()
            else
                follow()
        }
    */
    debugger
    return (
        <div className={s.users}>

        </div>
    )
}

export default User;